*telescope-glyph.nvim.txt*     For NVIM v0.8.0     Last change: 2024 August 05

==============================================================================
Table of Contents                     *telescope-glyph.nvim-table-of-contents*

1. telescope-glyph.nvim            |telescope-glyph.nvim-telescope-glyph.nvim|
  - Get Started        |telescope-glyph.nvim-telescope-glyph.nvim-get-started|
  - Usage                    |telescope-glyph.nvim-telescope-glyph.nvim-usage|
  - Configuraion      |telescope-glyph.nvim-telescope-glyph.nvim-configuraion|
  - TODO:                    |telescope-glyph.nvim-telescope-glyph.nvim-todo:|
  - Credit                  |telescope-glyph.nvim-telescope-glyph.nvim-credit|

==============================================================================
1. telescope-glyph.nvim            *telescope-glyph.nvim-telescope-glyph.nvim*

An extension for telescope.nvim
<https://github.com/nvim-telescope/telescope.nvim> that allows you to search
font glyphs



_Same as above but shows glyphs instead of emojis_


GET STARTED            *telescope-glyph.nvim-telescope-glyph.nvim-get-started*

Requirements: - Telescope <https://github.com/nvim-telescope/telescope.nvim>


INSTALL AND LOAD THIS PLUGIN: ~

Via packer.nvim <https://github.com/wbthomason/packer.nvim>

>lua
    use('AgatZan/telescope-glyph.nvim')
<

Via lazy.nvim <https://github.com/folke/lazy.nvim>

>lua
    return { 'AgatZan/telescope-glyph.nvim' } 
<

Add following to your Telescope config

>
    require('telescope').load_extension('glyph')
<


USAGE                        *telescope-glyph.nvim-telescope-glyph.nvim-usage*

>
    :Telescope glyph
<


CONFIGURAION          *telescope-glyph.nvim-telescope-glyph.nvim-configuraion*


TYPES ~

Glyph |= &#009 name: string &#009 value: string Inserted value &#009
description: string Extension Scheme | name|type | Description | Default | |
— |—|—|—| | action |fun( glyph: Glyph ):nil | Function that will be
called after choosing glyph | yank to reg | | base_glyph | boolean | Insert
|telescope-glyph.nvim-base-glyph| | true | | base_emoji | boolean | Insert
base_emoji
<https://github.com/xiyaowong/telescope-emoji.nvim/blob/master/lua/telescope-emoji/init.lua>
| false | | glyphs | Glyph[] | Your glyphs | nil |

**It’s optional.** by default

>lua
    telescope.setup {
        extensions = {
            glyph = {
                action = function(glyph)
                -- argument glyph is a table.
                -- {name="", value="", category="", description=""}
                    vim.fn.setreg("*", glyph.value)
                    print([[Press p or "*p to paste this glyph]] .. glyph.value)
                -- insert glyph when picked
                -- vim.api.nvim_put({ glyph.value }, 'c', false, true)
                end,
                base_glyph = true,
                base_emoji = false,
                glyphs = nil
            },
        },
    }
<


TODO:                        *telescope-glyph.nvim-telescope-glyph.nvim-todo:*

- ☐ Picker agnostic
- ☐ Thinking about table compilation


CREDIT                      *telescope-glyph.nvim-telescope-glyph.nvim-credit*

This project is a direct fork of telescope-glyph.nvim
<https://github.com/ghassan0/telescope-glyph.nvim> to provide font glyphs
instead of emojis.

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
